# Roadmap (Doc Pointer)

The authoritative roadmap lives at the repository root: [`../ROADMAP.md`](../ROADMAP.md). It's kept out of `docs/` so it's visible immediately from the GitHub repo view without a click.
